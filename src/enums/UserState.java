package enums;

public enum UserState {
    MAIN_MENU,
    BROWSING_PRODUCTS,
    SEARCHING,
    CART,
    ORDERING,
    AWAITING_PHONE,
    AWAITING_NAME,
    AUTHENTICATED,
    VIEWING_CART,
    DEFAULT
}